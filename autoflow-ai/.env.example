# Calendly
NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/christopher-do-doaisystems/30min
CALENDLY_WEBHOOK_SECRET=your_signing_key_from_calendly_dashboard

# CRM — inbound lead endpoint (website chat form + Calendly bridge forward here)
NEXT_PUBLIC_CRM_INBOUND_URL=https://crm.doaisystems.co.uk/api/leads/inbound

# CRM — public services endpoint (optional; fallback is hardcoded in Services.tsx)
# NEXT_PUBLIC_CRM_SERVICES_URL=https://crm.doaisystems.co.uk/api/services

# OpenAI (chatbot)
OPENAI_API_KEY=your_openai_api_key

# Notion (dynamic content + Monty context)
NOTION_TOKEN=your_notion_integration_token
NOTION_WORKSPACE_PAGE_ID=3248bfd6-0192-815f-81cb-ef244bb03950

# Supabase — REMOVED (runtime no longer writes to Supabase; historical data preserved)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# HubSpot — REMOVED (Roy added 2026-03-18, never adopted; CRM Postgres is now canonical)
# Also remove HUBSPOT_ACCESS_TOKEN from Vercel project settings post-merge.
# HUBSPOT_ACCESS_TOKEN=
